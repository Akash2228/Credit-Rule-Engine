package com.prayaan.credit_policy_engine.Enum;

public enum RuleDecision {

    APPROVED("APPROVED"),
    NEEDS_REVIEW("NEEDS_REVIEW"),
    REJECTED("REJECTED");

    RuleDecision(String approved) {
    }
}
